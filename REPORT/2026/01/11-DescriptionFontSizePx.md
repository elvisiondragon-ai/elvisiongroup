Assuming a default base font size of `16px` for the browser (as no explicit `font-size` is set on the `body` or `html` elements), here are the approximate font sizes in pixels:

*   **Subtitle in the header (`.header .subtitle`):** `1.4em` ≈ **22.4px**
*   **First paragraph below the `<h1>` in the header:** `1.2em` ≈ **19.2px**
*   **Second paragraph below the `<h1>` in the header:** `1.1em` ≈ **17.6px**
*   **First paragraph below the `<h3>` in the `.cta-box`:** `1.1em` ≈ **17.6px**
*   **Second paragraph below the `<h3>` in the `.cta-box`:** `1.1em` ≈ **17.6px**
